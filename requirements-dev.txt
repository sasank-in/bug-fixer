# Development and demo dependencies.
#
# Mirrors the [dev] and [demo] extras in pyproject.toml. Equivalent to
# `pip install -e ".[dev,demo]"`, which is the recommended install.

-r requirements.txt

# -- test and lint ---------------------------------------------------------

pytest>=8.0
# asyncio_mode = "auto" in pyproject lets async tests run without a decorator.
pytest-asyncio>=0.23
ruff>=0.6

# -- demo target -----------------------------------------------------------
# examples/vulnerable_api/ is the deliberately buggy API the end-to-end tests
# fuzz. Not needed to run AutoQA against your own service.

fastapi>=0.110
uvicorn>=0.29
# Used directly by the demo app's request models. FastAPI depends on it too,
# but the demo imports it by name, so declare it rather than rely on that.
pydantic>=2.0
