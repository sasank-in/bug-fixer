# Runtime dependencies for AutoQA.
#
# pyproject.toml is the source of truth — `pip install -e .` reads it and is the
# recommended install. This file exists for environments that expect a
# requirements file (CI images, Docker layers, pip-audit, some conda flows) and
# must be kept in step with the [project] dependencies there.
#
# Dev and demo extras live in requirements-dev.txt.

# Async HTTP client used to send every fuzz request. Also the source of the
# URL/query encoding that reproducers are built from, so the report and the
# executor cannot disagree.
httpx>=0.27,<1.0

# Parses OpenAPI specs served as YAML rather than JSON.
PyYAML>=6.0
